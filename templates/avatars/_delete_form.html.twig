<form method="post" action="{{ path('app_avatars_delete', {'id': avatar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avatar.id) }}">
    <button class="btn btn-primary">supprimer</button>
</form>
